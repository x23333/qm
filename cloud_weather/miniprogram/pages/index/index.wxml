
<block wx:for="{{products}}" wx:key="item._id">
  <van-card price="{{item.price}}" 
  desc="{{item.color}}" 
  title="{{item.title}}" 
  thumb="{{item.image}}" 
  data-id="{{item._id}}" 
  bindtap="click"/>
</block>